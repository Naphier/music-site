# Obsolete Terraform deploy workflow template

The executable deploy workflow now lives at:

```text
.github/workflows/terraform-deploy.yml
```

This placeholder remains only to avoid losing review context in the pull request. It can be removed after the PR is merged if desired.
